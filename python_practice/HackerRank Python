print 'Hello, World.'
inputString = raw_input('Welcome to 30 Days of Code!')

# Complete the 'solve' function below.
#
# The function accepts following parameters:
#  1. DOUBLE meal_cost
#  2. INTEGER tip_percent
#  3. INTEGER tax_percent
#

# Declare second integer, double, and String variables.
a = int
b = float
c = str
 
# Read and save an integer, double, and String to your variables.
# for input to store user's input instead of just the function, it's (input())
a = int(input())
b = float(input())
c =  str(input())
# Print the sum of both integer variables on a new line.
print (i + a)

# Print the sum of the double variables on a new line.
print (d + b)
# Concatenate and print the String variables on a new line
print (s+c)

class Person:
    def __init__(self,initialAge):
        # Add some more code to run some checks on initialAge
        #An obj attribute is a variable (property or field) stored on an object which in this case is class person.
        #Take the value passed into the constructor (initialAge) and store it in the object's age attribute.   
        # __init__ initializes the attributes of a newly created object.
        # self refers to the object being created.
        # initialAge is a parameter passed into the constructor.
        # self.age creates an attribute on the object and stores initialAge in it.     
        #self is the object
        # initialAge is a parameter supplied by the caller
        #self is not the object's name. It's a variable that refers to the object while the method is running
       
        #python math
        #p = Person(15)
        #Person.__init__(p, 15)
        
        #Class: Person
        #Object: instance created from the class; so obj is the singular person the script is refering to
        #Attribute: self.age = 25
        #Person = the blueprint (class)
        #p = one specific Person (object / instance)
        #that object represents a single individual
        if initialAge < 0:
            self.age = 0 
            print ('Age is not valid, setting age to 0.')
        
        else:
            self.age = initialAge
        
    
    def amIOld(self):
        # Do some computations in here and print out the correct statement to the console
        if self.age < 13:
            print('You are young.')
        elif 13<= self.age <18:
            print ('You are a teenager.')
        else:
            print('You are old.')
    def yearPasses(self):
        # Increment the age of the person in here
        self.age += 1

t = int(input())
for i in range(0, t):
    age = int(input())         
    p = Person(age)  
    p.amIOld()
    for j in range(0, 3):
        p.yearPasses()       
    p.amIOld()
    print("")



# The 's' variable above should be printed first.

def solve(meal_cost, tip_percent, tax_percent):
    # Write your code here
    # keep print in the function
    tip_percent = meal_cost * (tip_percent/100)
    tax_percent = meal_cost * (tax_percent/100)
    
    total = meal_cost + tip_percent + tax_percent

    print (int(round(total)))

if __name__ == '__main__':
    meal_cost = float(input().strip())

    tip_percent = int(input().strip())

    tax_percent = int(input().strip())

    solve(meal_cost, tip_percent, tax_percent)


if __name__ == '__main__':
    N = int(input().strip())
    
if N % 2 !=0:
    print ('Weird')
if N % 2==0 and 2 <= N <=5:
    print ('Not Weird')
if N % 2==0 and 6 <= N <=20:
    print('Weird')
if N % 2==0 and N > 20:
    print('Not Weird')

Loops and Range:
if __name__ == '__main__':
    n = int(input().strip())

for i in range(1,11):
    results= n * i
    print(n,"x",i,"=", results)

    
if-- defines first condition(boolean expression for condition
the cinditio is true, run, otherwise skip)
1. only one if statement per chain and it must always come first (a libra)
if can stand alone

else:   (coden=pendent)
    comes at the end
    No conditions
    optional
    cannot stand alone


elif: (interdependent)
    (else if)-- if I get this condition, then do that
    also have independednt ifs
    always comes after the else statement
    can use as many as you want
    needs conditions
    optional

if __name__ == '__main__':
    n = int(input().strip())

    
if n %2 != 0:
    print("Weird") 
elif n %2 == 0 and n>=2 and n<=5:
    print("Not Weird")
elif n %2 == 0 and n>=6 and n<=20:
    print("Weird")
elif n %2 ==0 and n>20:
    print("Not Weird")



if __name__ == '__main__':
    a = int(input())
    b = int(input())
    
    
    sum = sum([a,b])
    print(sum)
    
    diff = (a)-(b)
    print(diff)
    
    prod = (a)*(b)
    print(prod)
    
